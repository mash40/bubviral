GRAPHQL_ENDPOINT="https://bubsolar.com/graphql/"

export async function getStaticProps() {

var baseURL = 'http://' + process.env.CMS_URL + '/';

var myURL = new URL('graphql', baseURL);

const res = await fetch(myURL, ...)
